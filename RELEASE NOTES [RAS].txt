


a)	
Adicionado o suporte às métricas de Interval e Threshold para o tipo Double, visto que a versão original trata somente a métrica “Equal” para o referido atributo. 

Este item demandou ajustes nos métodos de comparação das classes Interval.java e Threshold.java.


1) Implementa support for Interval and Threshold metrics using Double, since the original version accepts only the "Equal" metric for that datatype. It has required some adjusts in classes Interval.java and Threshold.java.




b)	
Corrigido o bug na classe “PlainTextConverter” que impedia a aplicação dos “user data types” para a declaração de tipos enumerados de dados com uso na forma de símbolo ordenado na estrutura de atributos do caso. O documento XML “datatypes.xml” em “config\user” armazena a declaração dos tipos, como ilustra a Figura 4.
c)	
Corrigido um bug na edição de tipos double no formulário de campos da Query. A edição não formatava corretamente os valores de ponto flutuante e truncava parte de números maiores após a edição. O ajuste foi realizado na classe “DoubleEditor”.
d)	
Implementado o cálculo da métrica de similaridade global (weighted nearest neighbor), considerando corretamente os pesos dos atributos.
